#include <cmath>
#include <iostream>
#include <iomanip>
#include <string>

int main() {

    std::string input{ "" };
    std::getline(std::cin, input);

    while(input != "") {

    }

    return(0);
}